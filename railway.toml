[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=src uvicorn secql_api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
